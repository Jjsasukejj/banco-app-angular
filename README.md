# Banco App – Frontend

Aplicación frontend desarrollada en **Angular** para la gestión de clientes, cuentas, movimientos y generación de reportes bancarios.  
Consume la API REST desarrollada en .NET expuesta en el backend.

---

## 🚀 Tecnologías

- Angular 21
- TypeScript
- HTML / CSS
- RxJS
- Node.js + npm

---

## 📋 Requisitos

Antes de ejecutar el proyecto asegúrese de tener instalado:

- Node.js (versión LTS recomendada)
- npm
- Angular CLI (`npm install -g @angular/cli`)

---

## ▶️ Ejecución del proyecto

### 1️⃣ Instalar dependencias

Desde la raíz del proyecto frontend:

```bash
npm install

### Levantar la plicacion

```bash
ng serve

La aplicacion estará disponible en: http://localhost:4200
